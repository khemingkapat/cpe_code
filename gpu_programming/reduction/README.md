# README
everything is quite simple, just run with give command on the `Makefile` and that's basically

# Necessary file
1. `Makefile`
2. `reduction.cu`

these are the 2 files that is needed to run on any environment, copy these 2 files into your environment and then run

```bash
make ARCH=<your_arch> run
```
and that would produce the result that you could tinkering around the control panel first and we got the result.
