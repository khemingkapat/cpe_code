# README
everything is quite straightforward, since it  is already premade. That's very nice  of the instructor <3

but one  thing that I fixed is the `common.cuh` since both environment, university cluster and COLAB resulting in the error, so I fixed it and attached it there also